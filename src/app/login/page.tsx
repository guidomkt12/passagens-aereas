'use client'; import { useState } from 'react';
export default function Login(){const [error,setError]=useState(''); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const password=new FormData(e.currentTarget).get('password');const r=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({password})});if(r.ok)location.href='/dashboard';else setError((await r.json()).error)} return <main className="shell" style={{maxWidth:460,paddingTop:110}}><section className="card"><span className="tag">RADAR AÉREO</span><h1>Seu radar pessoal de passagens</h1><p className="muted">Acesso reservado ao proprietário.</p><form onSubmit={submit} className="grid"><label>Senha<input className="input" name="password" type="password" required autoFocus/></label>{error&&<p role="alert">{error}</p>}<button className="btn">Entrar com segurança</button></form></section></main>}
