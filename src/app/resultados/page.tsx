import { Nav } from '@/components/nav'; import { brl } from '@/lib/format'; import { compareMiles } from '@/lib/miles';
export default function Resultados(){const cmp=compareMiles(3400,80000,420,28);return <main className="shell"><Nav/><h1>Resultados</h1><p className="muted">Preço e disponibilidade devem ser revalidados antes de qualquer decisão.</p><section className="grid cols-2"><article className="card"><span className="tag">DINHEIRO · SerpApi</span><h2>GRU → LIS</h2><strong>{brl(3400)}</strong><p>LATAM · 1 escala · 12h40</p><button className="btn">Consultar em nova aba</button></article><article className="card"><span className="tag">MILHAS · opcional</span><h2>80.000 pontos + {brl(420)}</h2><p>Valor por milheiro: {brl(cmp.valuePerThousand)} · {cmp.recommendation}</p><p className="muted">Transferências de pontos normalmente são irreversíveis.</p></article></section></main>}
