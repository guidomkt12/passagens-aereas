import { Nav } from '@/components/nav'; export default function Configuracoes(){const rows=[['SerpApi / dinheiro','Configurado pelo n8n'],['Travelpayouts / tendências','Configurado pelo n8n'],['Seats.aero','Desativado por feature flag'],['Browser fallback','Desativado por feature flag']];return <main className="shell"><Nav/><h1>Configurações e conectores</h1><section className="grid">{rows.map(([name,status])=><article className="card" key={name}><strong>{name}</strong><p className="muted">{status}</p></article>)}</section><p className="muted">Segredos e URLs de webhook nunca são exibidos no navegador. Orçamento mensal depende das contas dos provedores.</p></main>}
