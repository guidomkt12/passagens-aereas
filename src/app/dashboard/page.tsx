import { Nav } from '@/components/nav'; import { brl } from '@/lib/format';
const deals=[['imperdivel','GRU → LIS',2980,'-31% vs. média'],['excelente','CGH → SCL',1194,'direto · 3 assentos'],['boa','GIG → MIA',2140,'fonte atualizada hoje']];
export default function Dashboard(){return <main className="shell"><span className="tag">RADAR AÉREO · PESSOAL</span><Nav/><h1>Bom dia. O céu está favorável.</h1><p className="muted">Oportunidades simuladas até que você conecte seus webhooks n8n.</p><section className="grid cols-3">{deals.map(([label,route,price,detail])=><article className="card" key={route as string}><span className="tag">{label as string}</span><h2>{route as string}</h2><strong style={{fontSize:26}}>{brl(price as number)}</strong><p className="muted">{detail as string}</p></article>)}</section><section className="grid cols-2" style={{marginTop:16}}><article className="card"><h2>Fontes</h2><p>SerpApi e Travelpayouts aguardando configuração segura.</p><span className="tag">Seats.aero desativado</span></article><article className="card"><h2>Últimas buscas</h2><p className="muted">Nenhuma busca persistida. Toda persistência passa pelo n8n.</p></article></section></main>}
