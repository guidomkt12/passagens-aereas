export function compareMiles(cashPrice: number, points: number, taxes = 0, costPerThousand = 0) { const valuePerThousand = ((cashPrice - taxes) / points) * 1000; const estimatedMilesCost = (points / 1000) * costPerThousand; const savings = cashPrice - taxes - estimatedMilesCost; return { valuePerThousand, estimatedMilesCost, savings, recommendation: savings > cashPrice * .05 ? 'milhas' : savings < -cashPrice * .05 ? 'dinheiro' : 'indiferente' as 'milhas'|'dinheiro'|'indiferente' }; }
